# 旧分離版LP（削除予定）

このフォルダは、ABテストを検討していた時期に分離してしまったNext.js版LPです。改善内容は現行LPへ統合済みのため、削除予定です。

現在の公開・編集対象ではありません。現行LPのソースはリポジトリ直下の静的HTMLです。

- メインLP: `../index.html`
- AI集中キャンプLP: `../training.html`
- 共通スタイル: `../style.css`
- フォーム共通部品: `../assets/contact-form.js`
- API: `../api/`

## 現在の扱い

- ABテストは中止しています。
- A/B/C案として分離していた訴求は、現行LPへ統合済みです。
- `public/images` の画像素材は `../assets/` に保存済みです。
- Vercelのデプロイ対象からは、ルートの `.vercelignore` で `landing-page-2/` を除外しています。
- このフォルダは新規編集・参照追加を行わず、次の整理タイミングで削除します。

## 注意

このフォルダ内の `/variant-a`, `/variant-b`, `/variant-c`, `/two-days` は現行導線ではありません。

今後本文や導線を直す場合は、このフォルダではなくリポジトリ直下のHTMLを編集してください。

削除前に確認すること:

- 現行LP側で必要な文言が `../index.html` と `../training.html` に入っている
- 必要な画像が `../assets/` に保存されている
- 本番導線がこのフォルダ内のページを参照していない
